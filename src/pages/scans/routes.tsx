// STUB — DELETE AT STITCH TIME. Agent 3 replaces this file at this exact path.
import type { RouteObject } from "react-router-dom";
import type { NavItem } from "@/lib/routes";

export const scansRoutes: RouteObject[] = [
  { path: "/scans", element: <div className="p-8">Scan history, coming soon.</div> },
];
export const scansNavItems: NavItem[] = [];
