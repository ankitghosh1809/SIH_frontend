# Agent 6 — scope note

Everything in this folder, plus the Task 1 stub files (marked `STUB — DELETE AT STITCH TIME`
in `src/types/api.ts`, `src/lib/routes.ts`, `src/lib/api-client.ts`,
`src/contexts/AuthContext.tsx`, `src/components/RiskBadge.tsx`, `src/components/EmptyState.tsx`,
`src/components/ErrorState.tsx`, `src/components/ProtectedRoute.tsx`), is the actual deliverable.

Everything else at the repo root and under `src/components/ui/`, `src/lib/utils.ts` (config
files, `App.tsx`, `main.tsx`, hand-authored shadcn-style primitives) is a standalone dev
harness built only so this branch can `npm run build` / `npm run dev` in isolation before
Agent 1's real project scaffold exists. See `HANDOFF_NOTES.md` at the repo root for the full
breakdown and stitching instructions.
