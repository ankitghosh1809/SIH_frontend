// STUB — DELETE AT STITCH TIME. Agent 4 replaces this file at this exact path.
import type { RouteObject } from "react-router-dom";
import type { NavItem } from "@/lib/routes";

export const patientsRoutes: RouteObject[] = [
  { path: "/patients", element: <div className="p-8">Patient registry, coming soon.</div> },
];
export const patientsNavItems: NavItem[] = [];
