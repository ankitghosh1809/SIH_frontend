// STUB — DELETE AT STITCH TIME. Agent 5 replaces this file at this exact path.
import type { RouteObject } from "react-router-dom";
import type { NavItem } from "@/lib/routes";

export const reviewRoutes: RouteObject[] = [
  { path: "/referrals", element: <div className="p-8">Doctor review and referrals, coming soon.</div> },
];
export const reviewNavItems: NavItem[] = [];
