// STUB — DELETE AT STITCH TIME. Agent 2 replaces this file at this exact path.
import type { RouteObject } from "react-router-dom";
import type { NavItem } from "@/lib/routes";

export const screeningRoutes: RouteObject[] = [
  { path: "/upload", element: <div className="p-8">Screening workflow, coming soon.</div> },
];
export const screeningNavItems: NavItem[] = [];
